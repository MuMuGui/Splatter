using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AudioReference : MonoBehaviour
{
    // Start is called before the first frame update
   public bool UIAudioStart;
    void Start()
    {
        UIAudioStart = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
